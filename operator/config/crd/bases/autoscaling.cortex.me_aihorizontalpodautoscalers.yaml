---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: aihorizontalpodautoscalers.autoscaling.cortex.me
spec:
  group: autoscaling.cortex.me
  names:
    kind: AIHorizontalPodAutoscaler
    listKind: AIHorizontalPodAutoscalerList
    plural: aihorizontalpodautoscalers
    singular: aihorizontalpodautoscaler
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: AIHorizontalPodAutoscaler is the Schema for the aihorizontalpodautoscalers
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AIHorizontalPodAutoscalerSpec defines the desired state of
              AIHorizontalPodAutoscaler
            properties:
              maxReplicas:
                type: integer
              minReplicas:
                type: integer
              targetDeploymentCPUThreshold:
                type: integer
              targetDeploymentName:
                type: string
              targetDeploymentNamespace:
                type: string
            required:
            - maxReplicas
            - minReplicas
            - targetDeploymentCPUThreshold
            - targetDeploymentName
            - targetDeploymentNamespace
            type: object
          status:
            description: AIHorizontalPodAutoscalerStatus defines the observed state
              of AIHorizontalPodAutoscaler
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
